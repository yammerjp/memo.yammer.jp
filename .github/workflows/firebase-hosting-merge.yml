name: Astro tests on push
'on':
  push:
    branches:
      - master
      - astro-migration
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: npm ci && npm run test:astro
