version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - ./:/app
      - /app/node_modules
    restart: always
    ports:
      - "4321:4321"
